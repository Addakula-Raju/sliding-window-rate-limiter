spring.application.name=rate-limiter

server.port=8080

# Rate limiter configuration: N requests per T seconds, per client.
# Change these two lines to reconfigure the limiter - no code changes needed.
ratelimit.max-requests=5
ratelimit.window-seconds=10
